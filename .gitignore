# ── 실제 업무 데이터 (절대 커밋 금지) ─────────────────
martin_stock.db
martin_chat.db
*.db
*.db-wal
*.db-shm
백업/

# ── 사용자 업로드 파일 ────────────────────────────────
ChatFile/
DayPhoto/
Image/

# ── 엑셀 원본 (용량 크고 업무 데이터) ─────────────────
doc/

# ── 빌드 산출물 (exe는 GitHub Releases로 배포, 저장소엔 안 넣음) ──
dist/
build/
재고관리.exe
재고관리_새버전.exe
업데이트적용.bat

# ── 파이썬 캐시 ───────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── 에디터/도구 ───────────────────────────────────────
.claude/
.vscode/
.idea/
*.log
